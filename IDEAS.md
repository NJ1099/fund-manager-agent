# 발전 아이디어

작성: 2026-08-23 · 코드베이스 전체를 읽고 정리한 것. 위에서부터 가치 순.

판단 기준은 하나다 — **"이 기능이 없으면 지금 보이는 성과를 믿을 수 없는가?"**
알파를 늘리는 아이디어보다, 지금 숫자가 진짜인지 확인하는 쪽이 먼저다.

---

## Tier 1 — 이게 없으면 나머지가 다 공중에 뜬다

### ~~1. 백테스트 하네스~~ ✅ 2026-08-24 완료

`core/backtest.py` + `core/cycle.py` + `core/infer_cache.py` + `core/sweep.py` +
`scripts/backtest.py`. 상세는 README '백테스트' 절과 HANDOFF 라운드 4.

핵심은 **전략 로직을 복제하지 않은 것**이다. 선별·최적화·게이트를 `core/cycle.py::plan`
하나로 뽑아 라이브와 백테스트가 같은 함수를 부른다. 복제했다면 백테스트는 실제로
돌아가는 전략이 아닌 것을 측정하게 됐을 것이다.

**실측**: 7사이클(6개월 · 월 1회) 첫 실행 100초 → 캐시 적중 재실행 0.3초.
파라미터 스윕 4개 조합이 각각 0.3~0.4초, 재추론 0회.

**첫 발견 — 성과가 `TOP_K` 에 매우 민감하다.** 2026-02~07 구간에서
TOP_K 2 → +13.2%(초과 +6.4%), 3 → +6.3%, 5 → +4.6%(초과 −2.3%), 7 → +3.3%.
단조 감소다. 다만 **TOP_K=2 로 바꾸자는 결론이 아니다** — 7사이클에서 2종목 집중은
한두 번의 베팅 결과이고, 승률이 50%로 떨어지며 낙폭은 8.4%로 커진다. 전형적인
과최적화 함정이다. 필요한 것은 더 긴 구간과 더 촘촘한 리밸런싱(`--step 5`)으로
같은 민감도가 유지되는지 보는 것.

**다음에 이 하네스로 답해야 할 질문** (전부 캐시 덕에 싸다):
- `--step 5`(주 1회) vs `--step 20`(월 1회) — 회전율 비용이 예측 우위를 잡아먹는가
- `MAX_TURNOVER` · `TURNOVER_HARD_LIMIT` 민감도 — 게이트가 성과를 지키는가 깎는가
- `SAMPLE_COUNT` 16 vs 32 — 경로를 늘리면 확신도가 쓸모 있어지는가 (재추론 필요)
- 여러 구간(2024·2025·2026)에서 같은 결론이 나오는가

---

### ~~2. 견해 적중률 추적 (모델 성적표)~~ ✅ 2026-08-28 완료 — **결과가 나빴다**

`core/validation.py` + `scripts/validate.py` + 대시보드 소개 탭의 성적표.
표본을 늘리려고 백테스트를 2024-01~2026-08 · 주 1회로 돌려 추론 캐시를 1,410건까지
채웠고, 그중 **1,400건**을 5거래일 뒤 실제 가격과 대조했다.

| 재는 것 | 결과 | 뜻 |
|---|---|---|
| 방향 적중률 | **51.1%** (기준선 58.0%) | 늘 "상승"으로 찍는 것보다 못하다 |
| IC (순위상관) | **-0.003** (t=-0.08) | 종목 간 순위에 정보가 **없다** |
| 확신도–적중 상관 | **+0.009** | 확신도는 적중률과 **무관하다** |
| 팬차트 커버리지 | **64.6%** (목표 90%) | 불확실성을 심하게 과소평가한다 |
| Brier 스킬 | **-0.253** | 상수 예측보다 나쁘다 |

원문 저자의 독립 검증(Kronos ≈ 무작위 걷기)이 우리 데이터에서도 재현됐다.

**확신도가 실제로 재고 있던 것.** 확신도와 실현변동성의 순위상관이 **-0.58**이다.
`dispersion_to_confidence` 는 경로 분산의 역순 정규화인데, 경로 분산은 그 종목의
변동성을 거의 그대로 따라간다. 그래서 확신도는 "이 예측이 믿을 만하다"가 아니라
**"이 종목이 조용하다"**를 재고 있었다. 이 시스템 전체가 "확신도가 높으면 더 크게
베팅한다"는 가정 위에 서 있었으므로, 그 근거가 사라진 셈이다.

**표본 크기가 결론을 바꿨다 — 기록해 둘 것.** 200건에서는 확신도 구간별 적중률이
높음 47.4% < 낮음 57.8% 로 **역전**이 보였고 상관도 -0.094 였다. 표본을 7배로 늘리자
상관이 +0.009 로 0 에 수렴했다. 진실은 "역전"이 아니라 **"무관"**이었다.
적은 표본에서 본 방향성 있는 패턴을 결론으로 삼지 말 것 — 이래서 `MIN_SAMPLES` 가 있다.

**같은 날 돌린 백테스트와 앞뒤가 맞는다.** 2024-01~2026-08 주 1회에서 누적 +34.1% vs
SPY +68.1%(초과 **-33.9%**)인데 최대낙폭은 5.9% vs 16.9% 로 **3분의 1**이다.
예측력이 없는 채로 저변동성 종목을 고르니 수익은 못 따라가고 낙폭만 작다 —
성적표가 말하는 것과 정확히 같은 그림이다.

---

### ~~2-b. 예측이 실제로 기여하는지 확인하기~~ ✅ 2026-08-28 완료 — **기여하지 않았다**

`core/baselines.py` + `scripts/backtest.py --scorer flat|random`.
`research_desk.scan(scorer=…)` 훅에 가짜 견해를 꽂아, 파이프라인은 한 줄도 바꾸지 않고
견해만 갈아끼웠다. 대조군은 추론이 없어 한 번에 5초다.

**같은 구간(2024-01-02 ~ 2026-08-27 · 주 1회 · 134사이클) 비교:**

| 견해 | 누적수익 | 초과 | 샤프 | 낙폭 | 회전율 |
|---|---|---|---|---|---|
| Kronos (현재) | +34.14% | −33.92% | 1.57 | 5.93% | 1.36% |
| **난수 10회 평균** | **+34.00%** | | **1.60** | | 1.62% |
| 난수 범위 | +29.56 ~ +36.92% (표준편차 2.25) | | 1.38~1.69 | | |
| 무견해 · 상위 5 편입 | +19.98% | −48.08% | 0.99 | 7.57% | 0.57% |
| **무견해 · 전 종목(TOP_K=10)** | **+34.12%** | −33.95% | **1.64** | 5.94% | 1.24% |
| SPY 매수 후 보유 | +68.07% | | | 16.88% | |

**세 가지가 나왔다.**

**① Kronos 는 난수와 구분되지 않는다.** 난수 분포에서 z = **+0.06** — 정확히 한가운데다.
난수 10회 중 **7회가 Kronos 보다 나았다.** 성적표(#2)의 IC = −0.003 과 정확히 일치한다.

**② 견해를 아예 없애도 성과가 같다.** 전 종목을 최적화기에 넘기면 +34.12%(샤프 1.64)로
Kronos(+34.14%, 샤프 1.57)와 사실상 동일하고 **샤프는 오히려 더 높다.**
즉 **사이클당 12~16초의 Kronos 추론은 값을 못 하고 있다.**

**③ 무견해·상위5(+19.98%)만 나쁜 이유는 견해가 없어서가 아니다.** 견해가 전부 같으면
안정 정렬 때문에 **워치리스트 앞 5개(SPY·QQQ·IWM·TLT·IEF)만 고정 편입**되고
GLD·SLV·XLE·XLF·XLK 가 영영 안 들어온다. 회전율도 0.57%로 주저앉는다.
**떨어진 것은 예측력이 아니라 종목 다양성이다.** 난수 견해는 매 사이클 순위를 섞어
그 다양성을 복원하고, 그것만으로 Kronos 와 같은 성과를 낸다.

**결론.** 이 시스템의 성과는 예측에서 오지 않는다. **10종목 유니버스에 대한 CVaR
최적화 + 저변동성 편향 + 종목 로테이션**에서 온다. 견해가 하는 일은 "종목을 5개로
좁히는 것"뿐이고, 좁히는 방식이 Kronos 든 난수든 차이가 없다.

**한계 (이 결론을 인용할 때 함께 적을 것).** 한 구간·한 유니버스(10종목)·상승장이고,
슬리피지가 없으며, 난수는 10회 표본이다. "Kronos 가 쓸모없음이 증명됐다"가 아니라
**"이 설정에서는 난수와 구분되지 않았다"**가 정확한 표현이다.

---

### ~~2-d. 예측 대신 설명 가능한 판정~~ ✅ 2026-08-28 완료

사용자 요청: "예상치를 통해 살지말지 고민을 하고 어떤 이유에 의해서 종목에 대한
추천 혹은 손절 해야되는 이유를 알고싶은데 구현할 수 있나? kronos 외에 다른거로"

`core/fundamentals.py` + `core/verdict.py` + 「내 종목」 판정 카드.
**맞히려 하지 않고 지금 상태를 재서 규칙으로 판정한다** — #2 와 #2-b 의 결론을
그대로 적용한 것이다.

**재료 (실측 가용성)**: 미국 개별주 17/17 · 한국 개별주 15/17 · 코스닥 14/17 ·
**ETF 0/17**. ETF 는 '결측'이 아니라 '해당 없음'으로 구분한다.

**설계 원칙 넷** (전부 `tests/test_verdict.py` 로 고정):
①사실(`fact`)과 예측(`forecast`)을 라벨로 가르고 예측 가중치를 낮춘다
②찬성·반대 근거를 항상 함께 낸다 ③극단값을 장점으로 세지 않는다
④손절선 셋(ATR·200일선·손실한도)을 다 보여주고 **이미 지난 선을 표시**한다.

**실측으로 걸러낸 함정 셋**: 목표가 +82.9%(주가 하락을 목표가가 못 따라온 것),
ROE 148.8%(자사주 매입으로 분모가 줄어든 것), 매출성장 +130%(기저효과).
전부 그대로 두면 "아주 좋은 종목"으로 보인다.

**남은 것**: 판정 규칙 자체가 아직 백테스트되지 않았다 — 아래 #2-e 참고.

---

### 2-e. 판정 규칙을 채점하기 ★ 다음 순위

`verdict.py` 의 문턱값(PER 12/30, ROE 15%, 부채 150%, ATR 3배, 200일선 …)은
도메인 상식에 기반한 것이지 **검증된 값이 아니다.** Kronos 에서 배운 것을 여기에
적용하지 않으면 같은 실수를 반복한다.

**방법.** `core/validation.py` 와 같은 구조로, 과거 시점의 판정을 만들고 그 뒤
N개월 수익률과 대조한다. 답해야 할 질문:

| 질문 | 재는 법 |
|---|---|
| "매수 검토" 종목이 "진입 보류"보다 실제로 나은가 | 판정 구간별 forward return |
| 점수와 수익률에 관계가 있는가 | 점수 vs 수익률 순위상관 (IC) |
| **손절선이 실제로 손실을 줄였는가** | 손절 규칙 적용 vs 미적용 최대낙폭 비교 |
| 애널리스트 목표가에 정보가 있는가 | 상승여력 구간별 실제 수익률 |

세 번째가 가장 값어치 있다. **손절은 이 모듈에서 예측이 필요 없는 유일한 부분**이라
검증도 가장 깨끗하게 된다. 그리고 결과가 나쁘면 문턱값을 고칠 근거가 생긴다.

**주의**: 펀더멘털은 과거 시점 값을 받기 어렵다(yfinance 는 현재 값만 준다).
그래서 1차로는 **가격 기반 신호만** 채점할 수 있다. 재무 신호까지 채점하려면
과거 재무 데이터 소스가 따로 필요하다 — 그 한계를 결과에 적을 것.

---

### 2-c. 그래서 무엇을 할 것인가

②가 사실이라면 선택지는 셋이고, **셋 다 지금 코드로 바로 잴 수 있다.**

**(가) 추론을 빼고 최적화기만 남긴다.** 사이클이 12~16초에서 1초 미만이 되고,
`torch`·`Kronos` 의존이 사라진다. 그러면 Vercel 에도 봇을 올릴 수 있다
(지금 못 올리는 이유가 torch 용량과 추론 시간이다). **잃는 것은 견해 화면과 팬차트다.**

**(나) 유니버스를 넓힌다.** 성과가 최적화기에서 온다면 재료(종목)를 늘리는 것이
모델을 키우는 것보다 직접적이다. 10 → 20~30종목으로 늘리고 `--scorer flat --set TOP_K=<전체>`
로 재보면, 예측 없이 얼마나 개선되는지 바로 나온다.

**(다) 견해의 쓰임새를 바꾼다.** 지금은 견해로 **종목을 고른다**. 그런데 IC 가 0 이므로
그 고르기에 정보가 없다. 대신 견해를 **노출 조절**(전부 하락 견해면 현금 비중↑)에만
쓰는 편이 남은 정보를 쓰는 방법일 수 있다 — IDEAS #7 과 이어진다.

어느 쪽이든 **SPY 를 33%p 밑도는 문제는 그대로 남는다.** 저변동성 편향이 상승장에서
구조적으로 지는 원인이므로, 여기를 건드리지 않으면 대조군을 아무리 돌려도 같은 자리다.

---

### 3. 슬리피지 · 체결 모델 ★ 백테스트가 생겨서 값어치가 커졌다

**문제.** 지금은 시장가가 **종가에 전량 체결**된다고 가정한다. 실제보다 유리하다.
회전율이 높은 전략일수록 이 낙관 편향이 커지므로, 회전율 게이트의 효과도 과소평가된다.

**최소 구현.**
```
체결가 = 종가 × (1 ± 스프레드/2 ± 충격)
  스프레드 : ETF 1~2bp (유동성 높은 SPY/QQQ) ~ 5bp (XLE 등)
  충격     : k × (주문금액 / 20일 평균 거래대금)
```
거래대금은 `data_desk` 가 이미 volume 을 받아오므로 추가 데이터가 필요 없다.

백테스트가 생긴 지금은 이게 더 중요해졌다. 하네스가 내놓는 성과가 전부 무슬리피지
가정 위에 있어서, 회전율이 높은 파라미터일수록 실제보다 유리하게 나온다 —
즉 **스윕이 회전율 높은 쪽을 부당하게 좋아 보이게 만든다.** `core/execution_desk.py`
의 체결 지점 한 곳만 고치면 라이브와 백테스트에 동시에 반영된다.

---

## Tier 2 — 알파

### 4. 확신도에 절대 기준 섞기 ★ 성적표로 근거가 생겼다

`dispersion_to_confidence` 는 워치리스트 안에서의 **상대** 정규화다. 시장 전체가
불확실한 날에도 최저 분산 종목은 항상 0.85를 받는다. "오늘은 전부 모르겠다"를
표현할 방법이 없다. → `경로 분산 / 실현변동성` 같은 절대 비율을 곱해서 보정.

**2026-08-28 추가.** 이건 이제 추측이 아니다. 성적표(#2)에서 확신도와 실현변동성의
순위상관이 **-0.58** 로 측정됐다 — 확신도는 사실상 "저변동성"의 다른 이름이었다.
경로 분산을 그 종목의 실현변동성으로 나누면 그 성분이 상쇄되고, 남는 것이
"이 종목치고 예측이 모였는가"라는 원래 묻고 싶었던 값이다.
고친 뒤에는 반드시 `scripts/validate.py` 로 다시 채점해서 확신도–적중 상관이
0 에서 움직이는지 확인할 것. **움직이지 않으면 그 보정도 값을 못 한 것이다.**

### 5. 더 큰 Kronos 모델 옵션
`Kronos-mini` 는 4.1M 파라미터다. 레포에 `base`(102M) 가 있다. CPU 추론 시간과
견해 품질의 트레이드오프를 백테스트(#1)로 재보면 된다. 설정 한 줄이면 바꿀 수 있다.

### 6. 다중 지평 앙상블
현재 5일 하나만 본다. 1일·5일·20일을 같이 뽑으면
- 지평 간 방향이 엇갈리는 종목 = 신호 약함 → 확신도 하향
- 일관되면 확신도 상향
추론 비용이 3배로 늘어나므로 #1 백테스트로 값어치를 먼저 확인할 것.

### 7. 하락 견해를 실제로 쓰기
지금 `AVOID` 종목은 그냥 편입 안 될 뿐, 정보가 버려진다. 롱온리 제약은 유지하되
- 하락 견해가 강하면 **현금 비중을 늘리는** 신호로 쓰거나
- 인버스/방어 ETF(SH, TLT 등)를 워치리스트에 넣어 표현
전자가 단순하고 안전하다.

---

## Tier 3 — 리스크

### 8. 변동성 타겟팅
현재는 항상 만액 투자(현금 0%)다. 목표 연변동성(예: 10%)을 정하고 최근 실현변동성에
맞춰 노출을 조절하면, 낙폭이 사후 대응(리스크 게이트)이 아니라 사전 관리가 된다.
게이트는 사고가 난 뒤 작동하지만 변동성 타겟팅은 사고 크기 자체를 줄인다.

### 9. 상관 급변 감지
위기에는 상관이 1로 수렴해서 분산 효과가 사라진다. 공분산 행렬의 최대 고유값 비중이
급등하면 경고 → 노출 축소. CVaR 최적화가 과거 상관을 믿는 한계를 보완한다.

### 10. 포지션별 손절
현재 손절 개념이 없다. 개별 종목이 -15% 가면 다음 사이클까지 기다린다.

---

## Tier 3.5 — 시장 브리핑 (2026-08-28 신설)

`core/macro_desk.py`·`news_desk.py`·`macro_brief.py` 로 지표 27종·뉴스 11개 소스·
주제별 코멘트/대응이 붙었다. 이어서 할 만한 것들:

| 항목 | 내용 |
|---|---|
| 보유 종목과 연결 | 지금 브리핑은 시장만 본다. "당신의 보유 중 이 주제에 걸리는 종목"을 짚어주면 값이 커진다. **단, 봇 장부와 섞지 말 것** — 읽기만 한다 |
| 브리핑 이력 | 매일 저장해두면 "지난주에 뭐라고 했는가"를 되짚을 수 있다. 코멘트 규칙의 사후 검증도 가능해진다 |
| 텔레그램 발송 | 워크스페이스에 봇 인프라가 있다. 아침에 브리핑 요약을 보내는 것 |
| 지표 추가 | 신용스프레드 실측(현재는 HYG/LQD 비율로 대용), 실질금리(TIPS), 원자재 지수(DBC) |
| 코멘트 규칙 검증 | 지금 규칙은 도메인 상식에 기반한 것이지 백테스트된 것이 아니다. "구리/금이 오르면 장기금리도 올랐는가" 같은 것을 실제로 재보면 규칙을 다듬을 수 있다 |

**하지 말 것 — 뉴스를 LLM 으로 요약하기.** 화면은 예뻐지지만 새로고침마다 돈이 나가고,
요약은 원문에 없는 확신을 만들어낸다. 제목을 그대로 싣고 분류만 하는 지금 방식을 유지할 것.

---

## Tier 4 — 운영 · UX

| 항목 | 내용 |
|---|---|
| 텔레그램 알림 | 워크스페이스에 봇 인프라가 이미 있다. 사이클 완료·게이트 발동·사이클 실패 알림 |
| 미국 공휴일 캘린더 | `schedule` 모드가 휴장일에도 돈다. `pandas_market_calendars` 로 해결 |
| 락 파일 | `server.py` 사이클 실행과 `bot.py loop` 동시 실행 시 상태 파일 경쟁 |
| 히스토리 드릴다운 | 과거 사이클을 대시보드에서 되짚어 보기 (지금은 최신 상태만) |
| 성과 기여도(attribution) | 수익이 어느 종목·어느 결정에서 나왔는지 분해 |
| 사이클 실패 알림 | `loop`/`schedule` 모드에서 예외가 나면 로그에만 남고 조용히 넘어간다 |

---

## 하지 말아야 할 것

- **브로커 연동** — 설계 의도상 이 저장소에 없어야 한다. 실거래는 리스크 한도·감사 로그·
  규제 검토가 전부 갖춰진 뒤에야 별개 문제로 다룰 것.
- **PM(LLM)에게 비중 결정 권한 주기** — 이 프로젝트의 핵심 안전장치다.
  Alpha Arena 에서 프런티어 LLM 6개 중 4개가 손실을 봤다.
- **비중 장부로 회귀** — 주식 수 + 현금 장부가 레버리지를 구조적으로 막는다.
  자세한 건 `CLAUDE.md` 의 "장부 불변식" 절.
- **백테스트에 전략 로직 복제하기** — `core/cycle.py::plan` 하나만 부를 것.
  자세한 건 `CLAUDE.md` 의 "백테스트 규율" 절.
- **스윕 최고점을 그대로 채택하기** — 사이클 수십 개짜리 백테스트에서 1등을 고르는 것은
  과최적화다. 스윕은 민감도를 보는 도구지 최적값 탐색기가 아니다.
