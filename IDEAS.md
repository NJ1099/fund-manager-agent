# 발전 아이디어

작성: 2026-08-23 · 코드베이스 전체를 읽고 정리한 것. 위에서부터 가치 순.

판단 기준은 하나다 — **"이 기능이 없으면 지금 보이는 성과를 믿을 수 없는가?"**
알파를 늘리는 아이디어보다, 지금 숫자가 진짜인지 확인하는 쪽이 먼저다.

---

## Tier 1 — 이게 없으면 나머지가 다 공중에 뜬다

### ~~1. 백테스트 하네스~~ ✅ 2026-08-24 완료

`core/backtest.py` + `core/cycle.py` + `core/infer_cache.py` + `core/sweep.py` +
`scripts/backtest.py`. 사용법은 README 의 '백테스트' 절 참고.

핵심은 **전략 로직을 복제하지 않은 것**이다. 선별·최적화·게이트를 `core/cycle.py::plan`
하나로 뽑아 라이브와 백테스트가 같은 함수를 부른다. 복제했다면 백테스트는 실제로
돌아가는 전략이 아닌 것을 측정하게 됐을 것이다.

**실측**: 7사이클(6개월 · 월 1회) 첫 실행 100초 → 캐시 적중 재실행 0.3초.
파라미터 스윕 4개 조합이 각각 0.3~0.4초, 재추론 0회.

**첫 발견 — 성과가 `TOP_K` 에 매우 민감하다.** 2026-02~07 구간에서
TOP_K 2 → +13.2%(초과 +6.4%), 3 → +6.3%, 5 → +4.6%(초과 −2.3%), 7 → +3.3%.
단조 감소다. 다만 **TOP_K=2 로 바꾸자는 결론이 아니다** — 7사이클에서 2종목 집중은
한두 번의 베팅 결과이고, 승률이 50%로 떨어지며 낙폭은 8.4%로 커진다. 전형적인
과최적화 함정이다. 필요한 것은 더 긴 구간과 더 촘촘한 리밸런싱(`--step 5`)으로
같은 민감도가 유지되는지 보는 것.

**다음에 이 하네스로 답해야 할 질문** (전부 캐시 덕에 싸다):
- `--step 5`(주 1회) vs `--step 20`(월 1회) — 회전율 비용이 예측 우위를 잡아먹는가
- `MAX_TURNOVER` · `TURNOVER_HARD_LIMIT` 민감도 — 게이트가 성과를 지키는가 깎는가
- `SAMPLE_COUNT` 16 vs 32 — 경로를 늘리면 확신도가 쓸모 있어지는가 (재추론 필요)
- 여러 구간(2024·2025·2026)에서 같은 결론이 나오는가

---

### 2. 견해 적중률 추적 (모델 성적표) ★ 이제 최우선

**문제.** 이 시스템 전체가 "확신도가 높으면 더 크게 베팅한다"는 가정 위에 서 있다.
그런데 **그 가정이 맞는지 확인하는 코드가 없다.** 원문 저자의 독립 검증에서 Kronos 는
무작위 걷기와 구분되지 않았다(Brier 0.189 vs 0.188). 우리 데이터에서도 같은지 봐야 한다.

**방법.** 매 사이클 예측을 저장해두고, `PRED_LEN`(5거래일) 뒤에 실제와 대조한다.

| 지표 | 무엇을 답하나 |
|---|---|
| 방향 적중률 | 상승/하락 방향이나 맞히는가 |
| IC (순위상관) | 종목 간 상대 순위를 맞히는가 — 포트폴리오에는 이쪽이 더 중요 |
| **확신도 구간별 적중률** | **확신도 0.8 구간이 0.3 구간보다 실제로 더 맞는가** |
| 팬차트 캘리브레이션 | 실제 가격이 5–95 구간 안에 90% 들어오는가 |

세 번째가 핵심이다. 확신도와 적중률에 상관이 없다면 `dispersion_to_confidence` 는
베팅 크기를 정할 근거가 못 되고, 그 사실을 아는 게 성과를 올리는 것보다 중요하다.

**구현.** `core/validation.py` + `state/predictions.jsonl`.
대시보드에 "모델 성적표" 섹션. 표본이 적을 때는 숫자를 내지 말고 "표본 부족"으로 둘 것.

**백테스트가 생겨서 이게 훨씬 싸졌다.** 라이브로는 표본 하나 쌓는 데 5거래일이 걸리지만,
`state/infer_cache.jsonl` 에는 이미 과거 시점 예측이 (종목 × 날짜)로 들어 있다.
캐시를 읽어 5일 뒤 실제 수익률과 대조하면 **추론 비용 0으로** 수백 개 표본이 나온다.
이 작업이 다음 순위인 이유다.

---

### 3. 슬리피지 · 체결 모델 ★ 백테스트가 생겨서 값어치가 커졌다

**문제.** 지금은 시장가가 **종가에 전량 체결**된다고 가정한다. 실제보다 유리하다.
회전율이 높은 전략일수록 이 낙관 편향이 커지므로, 회전율 게이트의 효과도 과소평가된다.

**최소 구현.**
```
체결가 = 종가 × (1 ± 스프레드/2 ± 충격)
  스프레드 : ETF 1~2bp (유동성 높은 SPY/QQQ) ~ 5bp (XLE 등)
  충격     : k × (주문금액 / 20일 평균 거래대금)
```
거래대금은 `data_desk` 가 이미 volume 을 받아오므로 추가 데이터가 필요 없다.

백테스트가 생긴 지금은 이게 더 중요해졌다. 하네스가 내놓는 성과가 전부 무슬리피지
가정 위에 있어서, 회전율이 높은 파라미터일수록 실제보다 유리하게 나온다 —
즉 **스윕이 회전율 높은 쪽을 부당하게 좋아 보이게 만든다.** `core/execution_desk.py`
의 체결 지점 한 곳만 고치면 라이브와 백테스트에 동시에 반영된다.

---

## Tier 2 — 알파

### 4. 확신도에 절대 기준 섞기
`dispersion_to_confidence` 는 워치리스트 안에서의 **상대** 정규화다. 시장 전체가
불확실한 날에도 최저 분산 종목은 항상 0.85를 받는다. "오늘은 전부 모르겠다"를
표현할 방법이 없다. → `경로 분산 / 실현변동성` 같은 절대 비율을 곱해서 보정.

### 5. 더 큰 Kronos 모델 옵션
`Kronos-mini` 는 4.1M 파라미터다. 레포에 `base`(102M) 가 있다. CPU 추론 시간과
견해 품질의 트레이드오프를 백테스트(#1)로 재보면 된다. 설정 한 줄이면 바꿀 수 있다.

### 6. 다중 지평 앙상블
현재 5일 하나만 본다. 1일·5일·20일을 같이 뽑으면
- 지평 간 방향이 엇갈리는 종목 = 신호 약함 → 확신도 하향
- 일관되면 확신도 상향
추론 비용이 3배로 늘어나므로 #1 백테스트로 값어치를 먼저 확인할 것.

### 7. 하락 견해를 실제로 쓰기
지금 `AVOID` 종목은 그냥 편입 안 될 뿐, 정보가 버려진다. 롱온리 제약은 유지하되
- 하락 견해가 강하면 **현금 비중을 늘리는** 신호로 쓰거나
- 인버스/방어 ETF(SH, TLT 등)를 워치리스트에 넣어 표현
전자가 단순하고 안전하다.

---

## Tier 3 — 리스크

### 8. 변동성 타겟팅
현재는 항상 만액 투자(현금 0%)다. 목표 연변동성(예: 10%)을 정하고 최근 실현변동성에
맞춰 노출을 조절하면, 낙폭이 사후 대응(리스크 게이트)이 아니라 사전 관리가 된다.
게이트는 사고가 난 뒤 작동하지만 변동성 타겟팅은 사고 크기 자체를 줄인다.

### 9. 상관 급변 감지
위기에는 상관이 1로 수렴해서 분산 효과가 사라진다. 공분산 행렬의 최대 고유값 비중이
급등하면 경고 → 노출 축소. CVaR 최적화가 과거 상관을 믿는 한계를 보완한다.

### 10. 포지션별 손절
현재 손절 개념이 없다. 개별 종목이 -15% 가면 다음 사이클까지 기다린다.

---

## Tier 4 — 운영 · UX

| 항목 | 내용 |
|---|---|
| 텔레그램 알림 | 워크스페이스에 봇 인프라가 이미 있다. 사이클 완료·게이트 발동·사이클 실패 알림 |
| 미국 공휴일 캘린더 | `schedule` 모드가 휴장일에도 돈다. `pandas_market_calendars` 로 해결 |
| 락 파일 | `server.py` 사이클 실행과 `bot.py loop` 동시 실행 시 상태 파일 경쟁 |
| 히스토리 드릴다운 | 과거 사이클을 대시보드에서 되짚어 보기 (지금은 최신 상태만) |
| 성과 기여도(attribution) | 수익이 어느 종목·어느 결정에서 나왔는지 분해 |
| 사이클 실패 알림 | `loop`/`schedule` 모드에서 예외가 나면 로그에만 남고 조용히 넘어간다 |

---

## 하지 말아야 할 것

- **브로커 연동** — 설계 의도상 이 저장소에 없어야 한다. 실거래는 리스크 한도·감사 로그·
  규제 검토가 전부 갖춰진 뒤에야 별개 문제로 다룰 것.
- **PM(LLM)에게 비중 결정 권한 주기** — 이 프로젝트의 핵심 안전장치다.
  Alpha Arena 에서 프런티어 LLM 6개 중 4개가 손실을 봤다.
- **비중 장부로 회귀** — 주식 수 + 현금 장부가 레버리지를 구조적으로 막는다.
  자세한 건 `CLAUDE.md` 의 "장부 불변식" 절.
- **백테스트에 전략 로직 복제하기** — `core/cycle.py::plan` 하나만 부를 것.
  자세한 건 `CLAUDE.md` 의 "백테스트 규율" 절.
- **스윕 최고점을 그대로 채택하기** — 사이클 수십 개짜리 백테스트에서 1등을 고르는 것은
  과최적화다. 스윕은 민감도를 보는 도구지 최적값 탐색기가 아니다.
