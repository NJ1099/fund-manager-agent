[pytest]
# 수집 범위를 tests/ 로 고정한다.
# Kronos/ 는 별도 클론한 원본 레포라 자체 테스트와 스크립트를 갖고 있는데,
# 그냥 `pytest` 를 돌리면 그쪽까지 수집하려다 matplotlib 미설치로 죽는다
# (우리 테스트는 하나도 안 돈 채로 "2 errors" 만 뜬다).
testpaths = tests
addopts = -q
